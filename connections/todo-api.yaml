# Connection definition for Todo API
name: todo-api-connection
type: CustomKeys
target: https://product-api-main-98dd1cf.zuplo.site
auth_type: ApiKey

# Credentials - use environment variable references for security
credentials:
  key: ${TODO_API_KEY}  # Will be read from environment variable

# Metadata (optional)
metadata:
  description: Connection to Todo API service
  environment: nonprod
